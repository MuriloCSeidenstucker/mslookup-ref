site_name: MSLOOKUP
repo_url: https://github.com/MuriloCSeidenstucker/mslookup-ref
repo_name: mslookup
edit_uri: tree/main/docs

theme:
  name: material
  language: pt-BR
  # logo: assets/logo.png
  # favicon: assets/logo.png

markdown_extensions:
  - attr_list

extra_css:
  - stylesheets/extra.css

plugins:
- mkdocstrings:
    handlers:
      python:
        paths: [mslookup_ref]